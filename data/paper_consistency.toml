# Machine-checkable consistency rules for the paper, enforced by bin/paper_check.py.
#
# WHY THIS FILE EXISTS
# Across blind-review rounds 6 to 8 the referees' blocking items were dominated by one
# class: a claim corrected in the body while a stale copy survived in a table, a caption or
# an appendix, and a percentage printed beside a numerator it does not divide. Every one of
# those is individually well formed, so no LaTeX or number gate sees it, and each round of
# fixes created the next round's findings. That is the same failure the corpus already has
# a control for (INST-1) pointed at the manuscript instead of the instruments.
#
# Six rule kinds, all declarative, all read by bin/paper_check.py:
#   [[retired]]      a claim that was withdrawn. It must not appear anywhere in the paper.
#   [[forbidden]]    vocabulary refused in a named file (the anti-SoK gate).
#   [[once]]         a claim that must appear in exactly one file.
#   [[contradiction]] a pair of phrases that must not both appear.
#   [duplicate]      no normalised window of N words may appear in two files (a single
#                    table, not an array: it has one window and one exempt list).
#   [[ratio]]        a printed percentage must equal its printed numerator over its printed
#                    denominator, both read from numbers.tex, within tolerance.

# ---------------------------------------------------------------- retired claims
[[retired]]
phrase = "absolute residual is about the same in both"
retired_in = "sec/fixes.tex, round 8"
allow_in = ["sec/fixes.tex"]   # the passage that performs the withdrawal quotes it
why = """The absolute residual at the isolated site and end to end were quoted as agreeing,
which presumes the two regions are comparable. The containment anomaly means they are not
known to be, so the cross-check was withdrawn in the body and must not survive elsewhere."""

[[retired]]
phrase = "The two bases also disagree"
retired_in = "sec/fixes.tex, round 8"
why = "Comparing the shapes of the two bases presumes the same comparability the paper withdrew."

[[retired]]
phrase = "this one cell of"
retired_in = "sec/fixes.tex, round 8"
why = """The phrase pointed at a wolfSSL measurement cell. Since revision A6 wolfSSL has a row
in tab:fixes, but its delta and MDE cells read 'not committed' and its exploitability 'not
graded', so there is still no measurement cell for prose to point at."""

[[retired]]
phrase = "of the call, \\mxResidualPerSig"
retired_in = "sec/fixes.tex, round 8"
why = "The site's tick figure was printed beside the end-to-end percentage, which divides a different numerator."

[[retired]]
phrase = "every label here is settled by arithmetic"
retired_in = "shared/front.tex, sec/intro.tex, revision A1"
why = """The universal covered the fix-site results too, which are statistical propositions
about one host at one budget and are not settled by any arithmetic. The restricted form is
now the only one, and it is itself tier-scoped: every recall-eligible (tier A/B) corpus pair
label is settled by a key recovery that verifies under the published public key, while the
three tier-C pairs carry a published recovery not rerun here and enter no recall
denominator; fix-site labels are statistical and carry their host and budget. The two kinds
are defined once in sec/method.tex and tabulated in sec/index.tex."""

[[retired]]
phrase = "positive label here is settled by arithmetic"
retired_in = "sec/intro.tex, revision A1"
why = "The introduction's copy of the same universal, with 'positive' in it."

[[retired]]
phrase = "certifies each label by arithmetic"
retired_in = "sec/method.tex, revision A1"
why = "The apparatus section's copy of the same universal; it now says corpus pair label."

[[retired]]
phrase = "Every corpus label is settled by arithmetic"
retired_in = "shared/front.tex, revision A8"
why = """The abstract's universal covered the three tier-C pairs, whose labels rest on a
published recovery not rerun in this artifact. The scoped form says recall-eligible."""

[[retired]]
phrase = "every corpus pair label is settled by a key recovery"
retired_in = "sec/intro.tex, revision A8"
why = """The Method paragraph's copy of the same universal: three of the nine corpus pairs
are tier C, no recovery ran here for them, and they enter no recall denominator. The
sentence now scopes the recovery claim to the recall-eligible pairs."""

[[retired]]
phrase = "each carrying a key recovery certified by arithmetic"
retired_in = "README.md, revision A8 retry"
why = """The README's opening framing quantified the recovery certificate over every
corpus pair; the three tier-C pairs carry a published recovery not rerun here, so the
sentence now names the recall-eligible pairs as the ones certified by arithmetic."""

[[retired]]
phrase = "with a single recall fraction"
retired_in = "sec/app-threats.tex, revision A8"
why = """tab:blindspot prints verdict words and its caption says recall fractions are
absent, a fraction over n=1 being a Bernoulli outcome dressed as a rate; the appendix
described the table's previous form."""

[[retired]]
phrase = "carry one recall row each"
retired_in = "sec/app-threats.tex, revision A8"
why = """varlat has two scored rows in tab:blindspot and binsec three; only the
recall-eligible row is single, and the heading now says so."""

[[retired]]
phrase = "carry a single row each"
retired_in = "sec/f1-blindspots.tex, revision A8"
why = """True only of recall-eligible rows: the two instruction-class tools also hold
tier-C rows, including the division detection that is the section's own headline."""

[[retired]]
phrase = "Every timing figure here is acquired"
retired_in = "sec/method.tex, revision A8"
why = """The paper's own Graviton figures are acquired on a rented aarch64 instance, so
the one-shared-host universal holds for the corpus dumps and fix cases only and now
names the exception."""

[[retired]]
phrase = "Every timing figure is acquired same-process"
retired_in = "sec/app-stats.tex, revision A8"
why = "The appendix copy of the same universal, scoped the same way."

[[retired]]
phrase = "and aarch64 only"
retired_in = "sec/intro.tex, revision A8"
why = """The emission map is the eight pinned x86_64 cells; the one aarch64 build sits
outside its discipline, unpinned and undigested, so the remit must not fold it in."""

[[retired]]
phrase = "Each committed arm is a single acquisition"
retired_in = "sec/method.tex, revision A8"
why = """The fix-case designs are committed three acquisitions per design with the
between-acquisition range printed, so the single-acquisition statement holds for the
corpus arms only and now says so."""

[[retired]]
phrase = "the committed dump in parentheses"
retired_in = "sec/fixes.tex, revision A8"
why = """'The committed dump', singular, predates the committed repeat acquisitions;
three dumps per design are committed and the parenthesised figure is the first
acquisition, which is what the caption now calls it."""

[[retired]]
phrase = "zero (the committed dump"
retired_in = "sec/app-measure.tex, revision A8"
why = "The appendix copy of the same singular, replaced by 'the first of the committed acquisitions'."

[[retired]]
phrase = "Each answer is a fix-site label"
retired_in = "sec/fixes.tex, revision A8"
why = """By the paper's own definition a fix-site label is the outcome of a statistical
test carrying an MDE; libgcrypt's row carries neither, its closure being decided by
source inspection, so only MatrixSSL's answer is a fix-site label."""

[[retired]]
phrase = "libgcrypt, carries both"
retired_in = "sec/method.tex, revision A8"
why = """libgcrypt carries a site-closure row settled by inspection plus a corpus pair
label settled by recovery, not a fix-site label, which needs a statistical test."""

[[retired]]
phrase = "division pair's on a synthetic amplified"
retired_in = "sec/intro.tex, revision A8"
why = """The ladder pair is a second exception: synthetic, scored, and carrying no
amplification, so the universal now excepts the division and ladder pairs."""

[[retired]]
phrase = "names the quantities it cannot"
retired_in = "shared/front.tex, revision A8"
why = """The body says twice that no printed quantity is unregenerable any longer, only
two claims; the abstract's promise pointed at an empty set and now names the claims."""

[[retired]]
phrase = "is byte-identical between"
retired_in = "sec/fixes.tex, revision A8"
why = """The evidence normalises addresses and relocation offsets before comparing, which
is instruction identity, not byte identity; no raw-byte digest was recorded, so the paper
must not name a check that was not run."""

[[retired]]
phrase = "in half of them from about"
retired_in = "sec/app-defs.tex, revision A8"
why = """'From about 3,000' implies at least half from 3,000 upward, but the committed
sweep recovers 3 of 8 at 4,000: the curve is not monotone at that resolution, so the
sentence now reports it pointwise."""

[[retired]]
phrase = "four verdict statuses"
retired_in = "sec/app-defs.tex, revision A8"
why = """The specification and bin/score.py carry five adapter verdict statuses;
'four' predates the separation of budget_exhausted from error."""

[[retired]]
phrase = "the test suite and the manuscript checks"
retired_in = "sec/app-defs.tex, revision A8"
why = """The four-gate enumeration of bin/verify_all.sh predates the fifth gate, the
headline-recall refusal, which the introduction singles out; the appendix now lists all
five."""

[[retired]]
phrase = "plus the five appendices"
retired_in = "main.tex and main-eprint.tex, revision A8"
why = "The eprint build inputs six appendices, A to F, since ep-related.tex was split out."

[[retired]]
phrase = "flat divider"
retired_in = "shared/front.tex, sec/f3-microarch.tex, sec/intro.tex, sec/f1-blindspots.tex, bin/index_table.py, bin/regen.py, README.md, results/kyberslash_graviton.json and pairs/kyberslash/graviton/measure_arm.py, revision A7"
why = """'Flat' asserted a property of the divider that no measurement here supports: the
serial-chain sweep on the acquisition host resolves no operand-dependent step at the
0.01-tick floor, while a per-call two-class test on the same host does resolve an
operand-magnitude effect. The claim is now stated with its resolution and its arrangement,
no operand-dependent step resolvable at \\hostNoiseFloor{} tick in a serial chain, and the
six-number table in sec/f3-microarch.tex carries the figures behind it."""

[[retired]]
phrase = "flat hardware"
retired_in = "sec/intro.tex, sec/f3-microarch.tex, revision A7"
why = "The introduction's and section 5's copies of the same unqualified 'flat'."

[[retired]]
phrase = "divider is flat"
retired_in = "sec/f1-blindspots.tex, sec/f3-microarch.tex, revision A7"
why = "Same claim in its predicate form; the resolution and the arrangement must travel with it."

[[retired]]
phrase = "latency is flat"
retired_in = "sec/f3-microarch.tex, results/kyberslash_x86_idiv.json and pairs/kyberslash/x86/measure.py, revision A7"
why = "The caption's copy of the same unqualified 'flat', and the x86 record note that seeded it."

[[retired]]
phrase = "invisible from in-process is invisible from anywhere"
retired_in = "sec/index.tex, revision A2"
why = """The claim ran the vantage licence the wrong way: a clean in-process verdict is bound
to its budget n, and a remote adversary is not bound to ours, so it licenses no claim about a
remote adversary at a budget this paper does not measure. Lemma 1 in sec/index.tex states
the licence precisely, in one direction only, and this wording must not return."""

[[retired]]
phrase = "the method declines to grade"
retired_in = "shared/front.tex, sec/intro.tex, revision A3"
why = """The wolfSSL outcome was stated as a refusal by the method, an inversion that reads as
the method's choice. The outcome is a fact about admission: wolfSSL does not meet the
admission rule (the two-sided oracle recovers nothing on the vulnerable arm) and is not
graded. The declarative is now the only form, in the abstract, the introduction and
sec/fixes.tex."""

[[retired]]
phrase = "declines to grade"
retired_in = "shared/front.tex, sec/intro.tex, revision A3"
why = "The same inversion without its subject."

[[retired]]
phrase = "decline to grade"
retired_in = "sec/fixes.tex, revision A3"
why = "The first-person copy of the same inversion in the fix section."

[[retired]]
phrase = "Do constant-time fixes hold?"
retired_in = "shared/front.tex, revision A3"
why = """The interrogative title did not match what is delivered; the reviewer asked for a
declarative title naming the indices and the three remediations, and the title is now that."""

[[retired]]
phrase = "of the signing path's own key generation inside the signature"
retired_in = "shared/front.tex, revision A3"
allow_in = ["sec/fixes.tex", "sec/app-measure.tex"]
why = """The abstract's containment sentence (the mxGenkeyGap figure) was replaced by the
clause 'measured on the routine called directly'; the containment argument itself is stated
once in sec/fixes.tex and detailed in the eprint measurement appendix, not in the abstract."""

[[retired]]
phrase = "the vantage strengthens a \\emph{clean} verdict"
retired_in = "sec/index.tex, revision A2"
why = "The same overclaim in its second half; see the entry above."

[[retired]]
phrase = "stronger vantage than that adversary has"
retired_in = "sec/index.tex, revision A2"
why = """The third form of the same claim: it read the in-process vantage as dominating the
remote one, which is the direction Lemma 1 refuses. The vantage is now one element of the
attack-model four-tuple, and the licence between vantages is stated by the lemma alone."""

[[retired]]
phrase = "is an effort boundary of this artifact, not a property of the tool"
retired_in = "bin/applicability_table.py caption, revision A8"
why = """The applicability caption filed all eight no-harness rows as an effort boundary,
but the four varlat and binsec rows on the ECDSA pairs are a measured incapacity of the
pinned images (results/tool_reach_limits.json): neither image can build an OpenSSL-linked
arm. The caption and those four reason strings in results/verdicts.jsonl now say so."""

[[retired]]
phrase = "an effort boundary, not a property of the pair"
retired_in = "results/verdicts.jsonl (varlat/binsec ECDSA rows), revision A8"
why = """As a description of the varlat and binsec rows on the ECDSA pairs this spelling
was refiled as a measured incapacity of the pinned images. It survives in the record only
for the ladderleak and hqc-reject rows, where it remains true; the paper must not quote it
for the measured rows, so any paper occurrence is a survivor to inspect."""

[[retired]]
phrase = "There are currently 55 tools in the table"
retired_in = "data/facts.toml, revision A8"
why = """The hand-typed ct-tools count retrieved from a web page was retired in favour of
nFieldIndexed, counted by bin/regen.py from results/analyser_matrix.json at the pinned
inventory commit; background.tex prints that macro and facts.toml no longer carries the
entry, so the retirement the regen.py comment records is now true."""

[[retired]]
phrase = "universally safe and none that is universally unsafe"
retired_in = "sec/f2-toolchain.tex, revision A8"
why = """False at the optimisation-level reading: within the measured cells the higher
optimisation levels are division-free under both compilers. The claim was narrowed to the
vendor-versus-level form, neither compiler safe at every level and no level unsafe under
both, only the size and zero levels emitting and which one depending on the compiler."""

[[retired]]
phrase = "no single build choice is universally safe"
retired_in = "pairs/kyberslash/pair.toml, revision A8"
why = """The manifest and emission-record spelling of the same universal, narrowed to the
same vendor-versus-level form in pairs/kyberslash/pair.toml and the finding field of
results/kyberslash_emission.json."""

[[retired]]
phrase = "null band calibrated on the constant-time negative sentinel"
retired_in = "docs/adapter-reach.md, revision A8"
why = """As a live decision rule this is PR-3, retired by PR-4: verdicts are decided
against a permutation null built from each run's own samples within its declared batches
(bin/dudect_permute.py), with Benjamini-Hochberg across the committed arms in
bin/score.py. The calibration files stay committed as the record of the retired rule and
are read by no verdict, so the paper must not present the band in the present tense."""

# ---------------------------------------------------------------- printed ratios
# num / den must equal pct. Macros are read from the generated numbers.tex, so this
# catches a rounded denominator that stops the printed arithmetic reproducing.
[[ratio]]
num = "gravDeltaTicks"
den = "gravCallTicks"
pct = "gravDeltaPercent"
tol = 0.15
why = "0.391 over a denominator printed as 2.1 gives 18.6%, not the 19.1% printed beside it."

[[ratio]]
num = "mxSigDelta"
den = "mxSigTicks"
pct = "mxResidualPerSig"
tol = 0.02
why = "The end-to-end residual must divide the end-to-end signature cost, not the isolated call."

[[retired]]
phrase = "the leak is attenuated"
retired_in = "main.tex, round 9"
why = """The attenuation factor was a ratio of two single acquisitions on separately built
arms on a host whose frequency is not pinned, so the within-run bootstrap intervals do not
bound it. Directions and class differences are reported instead."""

[[retired]]
phrase = "not the fivefold"
retired_in = "results/fix_verification.json and README, round 16"
why = """The withdrawn fivefold attenuation had been replaced by a 1.44x figure, which is the
same forbidden quantity: a ratio across two separately built releases. The record now gives
the two class differences with their between-acquisition ranges and no ratio."""

[[retired]]
phrase = "replication corroborates direction"
retired_in = "sec/fixes.tex, round 16"
why = """The one aarch64 run was acquired hours before the harness correction, so it timed
scalar multiplication on a different curve, and its dumps are uncommitted. No cross-host
replication of the MatrixSSL case is carried."""

[[retired]]
phrase = "reproduces on an unrelated microarchitecture"
retired_in = "sec/app-threats.tex, round 16"
why = """Same withdrawal as above: the second-microarchitecture measurement predates the harness
correction and does not corroborate the corrected finding."""

[[retired]]
phrase = "routine of indistinguishable cost"
retired_in = "sec/fixes.tex and shared/front.tex, round 16"
why = """The deployed call and the library's own key generation are about one per cent apart,
consistently in sign across every containment run, and the body prints that gap beside the
claim. A resolution word overstated it."""

[[retired]]
phrase = "the cross-check that the"
retired_in = "sec/fixes.tex, round 9"
allow_in = ["sec/fixes.tex"]
why = "Superseded by the containment arithmetic, which rules out the whole-signature region rather than merely noting a discrepancy."

[[retired]]
phrase = "The arithmetic settles which one fails"
retired_in = "sec/fixes.tex, round 13"
why = """7.7 instructions per tick is at or near peak retire width, sustained on
dependency-bound big-integer arithmetic, so the isolated region is not exonerated either.
All three figures are mutually impossible and which is wrong is undetermined."""

[[retired]]
phrase = "within what a wide core can retire"
retired_in = "sec/fixes.tex, round 13"
why = "Same overclaim: it presented an implausible retire rate as a plausible one."

[[retired]]
phrase = "reaches deployment is established by"
retired_in = "sec/fixes.tex, round 13"
why = "The abstract and conclusion say supported-not-established; section 3 must not say established."

# ---------------------------------------------------------------- relocations
# Added 2026-08-26 BEFORE the structural reframe, deliberately. Every worst finding of the
# thirteen blind-review rounds came from editing a claim and leaving a copy behind, and the
# reframe moves roughly 40% of the body text between files. Each rule below names a claim
# that CHANGES HOME: the phrase is legal in its destination (allow_in) and nowhere else, so
# a copy left in the source file fails the check.
#
# These rules FAIL until the corresponding move is finished. That is intended: they are the
# migration's own checklist, and paper_check goes green only when the last copy is gone.

[[retired]]
phrase = "The site is a choice, and every result is indexed by it"
retired_in = "sec/threats.tex -> sec/fixes.tex, restructure"
allow_in = ["sec/fixes.tex"]
why = """The clearest instance of the paper's thesis was filed as a threat to validity. It
becomes a result in the libgcrypt case and must not survive in the limitations section."""

[[retired]]
phrase = "The pilot used a fixed"
retired_in = "sec/method.tex -> sec/app-stats.tex, restructure"
allow_in = ["sec/app-stats.tex"]
why = "The two-rule tau post-mortem is appendix material; only the corollary stays in the body."

[[retired]]
phrase = "binaries reproducing their recorded digest"
retired_in = "sec/method.tex -> sec/f2-toolchain.tex, restructure"
allow_in = ["sec/f2-toolchain.tex"]
why = "BIN-1 is what makes the emission map airtight; it belongs with the claim it certifies."

[[retired]]
phrase = "The adversary is the one each reproduced attack assumes"
retired_in = "sec/method.tex -> sec/index.tex, restructure"
allow_in = ["sec/index.tex"]
why = "The threat model instantiates the attack-model index and belongs with the index set."

[[retired]]
phrase = "Selecting the fastest"
retired_in = "sec/fixes.tex -> sec/app-measure.tex, restructure"
allow_in = ["sec/app-measure.tex"]
why = """The selection-contamination analysis belongs with the exploitability paragraph in the
measurement appendix and must not reappear in the body; since 2026-08-26 its numbers regenerate
from the committed 4-3-0 trace and key."""

[[retired]]
phrase = "rest on a trace this artifact does not carry"
retired_in = "sec/fixes.tex (tab:fixes, MatrixSSL row), round 5 of the fifth review"
why = """The cell said the diagnostics at the aucMatrixN budget rest on an uncommitted trace.
Since the round-4 re-emission that macro counts the committed 25,000-signature 4-3-0 trace,
whose key is committed beside it. The cell must not name a lattice attempt on the fixed
build either: the one lattice run in the record was key-ordered on the uncommitted
250,000-signature 4.2.1 pre-fix trace, so no timing-ordered recovery on the fixed build has
been attempted, and the cell says so."""

[[retired]]
phrase = "one lattice attempt, at a larger budget"
retired_in = "sec/fixes.tex (tab:fixes, MatrixSSL row), round 6 of the fifth review"
why = """The cell attributed a lattice attempt at a larger budget to the fixed build. The only
larger trace is the 250,000-signature one that _correction in results/exploit_budget.json
identifies as the 4.2.1 pre-fix build, and the only lattice run on it was key-ordered and
succeeded (pairs/matrixssl-minerva/evidence/mechanism_recovery.md). No timing-ordered lattice
attempt on any 4-3-0 trace is recorded, so 'recovery pending' means not attempted."""

[[retired]]
phrase = "recovery attempt was run at a budget larger than"
retired_in = "sec/fixes.tex (what is still not recomputable), round 6 of the fifth review"
why = """Same misattribution as the table cell: the budget it excused belonged to a key-ordered
run on the pre-fix trace, not to a recovery attempt on the fixed build."""

[[retired]]
phrase = "belongs to a trace the repository does not carry"
retired_in = "sec/app-threats.tex, round 5 of the fifth review"
why = """The reason INST-1 was live used to be that the committed MatrixSSL key was orphaned.
The key now labels the committed 4-3-0 trace. The live failure was not a key/trace mismatch
at all: the earlier revision read its figures from an uncommitted 250,000-signature trace that
its own signing key did label (that key, 0837b047...a07, was recovered later by the key-ordered
lattice run in pairs/matrixssl-minerva/evidence/mechanism_recovery.md and never committed; the
only committed key, signing-key-4-3-0.hex, labels the committed 25,000-signature 4-3-0 trace)
but that was the 4.2.1 pre-fix build (auc_full 0.6335 in
_superseded_matrixssl_arm of results/exploit_budget.json, not one half); the one-half reading
came only from a later diagnostic cross-pairing of the committed 4-3-0 key with a 4.2.1 trace,
and the appendix now keeps the two apart."""

[[retired]]
phrase = "estimator read one half rather than an error"
retired_in = "sec/app-threats.tex, round 6 of the fifth review"
why = """The appendix fused two events: the uncommitted 250,000-signature trace that the earlier
revision spent was correctly labelled by its own uncommitted signing key and read 0.6335, and the one-half
reading (0.5047) came from a later cross-pairing of that key with a 4.2.1 trace. Saying the
estimator read one half on the spent trace misattributes INST-1's failure class to an
incident it could not have caught."""

[[retired]]
phrase = "the committed key did label"
retired_in = "sec/app-threats.tex, round 7 of the fifth review"
why = """The uncommitted 250,000-signature pre-fix trace was labelled by its own signing key
(0837b047...a07, recovered by the key-ordered lattice run, never committed), not by the
committed signing-key-4-3-0.hex (292f2a08...74f9), which was generated in the run that
produced the committed 25,000-signature 4-3-0 trace. The two keys differ; the committed key
touched the pre-fix trace only in the later diagnostic cross-pairing that read one half."""

[[retired]]
phrase = "the pre-fix build reads"
retired_in = "sec/fixes.tex, round 5 of the fifth review"
why = """A single committed dump (mxEffectPrefix) was printed as the pre-fix counterpart of the
fixed build's three-acquisition mean. Both arms are now reported as means over the same three
acquisitions (mxRepPrefixMeanEffect against mxRepMeanEffect); a single-dump figure may appear
only labelled as the committed dump beside its mean."""

[[retired]]
phrase = "tool-by-item rows"
retired_in = "sec/method.tex -> sec/app-defs.tex, restructure"
allow_in = ["sec/app-defs.tex"]
why = "The applicability arithmetic goes where the grid already lives; the body keeps the rule."

[[retired]]
phrase = "remove} the secret-dependent operation"
retired_in = "sec/fixes.tex -> sec/index.tex, restructure"
allow_in = ["sec/index.tex"]
why = "The five-outcome taxonomy is the range of the site-closure axis, so it belongs with the formulation."

[[retired]]
phrase = "an instruction-class verdict is a proposition about the binary alone"
retired_in = "sec/app-defs.tex -> sec/index.tex, restructure"
allow_in = ["sec/index.tex"]
why = "The verdict semantics are the thesis in symbols and were sitting on page 28."

# ---------------------------------------------------------------- round 14
[[retired]]
phrase = "measures that divider constant time on this host independently of any statistical test"
retired_in = "sec/threats.tex, round 14"
why = """Asserted in three places that the direct microbenchmark settles the KyberSlash
division on this host without recourse to statistics, which reads as a demonstration that no
operand dependence exists. The chained sweep finds no operand-dependent step; a per-call
design that includes the call's surroundings resolves an effect we have not localised. The
clean dudect verdict is consistent with the arrangement dudect uses and is not a claim about
every arrangement."""

[[retired]]
phrase = "each pair's departures are recorded"
retired_in = "sec/threats.tex, round 14"
why = """A universal over a table with rows for six pairs. tab:divergence is scoped to the
recall-eligible pairs, so the claim must name that scope; the tier-C reproductions carry their
divergences in their manifests instead."""

[[retired]]
phrase = "no containers and no hardware"
retired_in = "sec/intro.tex, round 15"
why = """The split promised verification from a cold clone with nothing but the repository,
and that is false for five of the six recall-eligible pairs: their recovery runs inside the
pinned image, because lattice reduction needs fpylll and upstream's curve module is GPL and
is kept out of this MIT tree. What is true is no analyser, no measurement and no hardware.
sec/method.tex states the container cost once and every other site points at it."""

[[retired]]
phrase = "against\nnothing but the repository"
retired_in = "sec/method.tex, round 15"
why = "The same stdlib-only overclaim in the section that defines the split."

[[retired]]
phrase = "the recall-eligible corner"
retired_in = "sec/method.tex, round 15"
why = """tab:blindspot prints the corpus pairs carrying at least one applicable analyser, not
the recall-eligible ones. Three of its rows are tier C and outside every recall denominator,
and three recall-eligible pairs have no row in it at all. Calling it the recall-eligible corner
invites a reader to divide its detections by its rows and read the result as recall."""

# ---------------------------------------------------------------- sentence length
# A reviewer's cap: about 35 words, one claim per sentence. Reported as a warning with
# the count per file until the writing pass brings the body under it; then fail = true.
# The cap is hard over the body and the abstract (revision A9 brought them to zero over
# it). The eprint-only appendices are exempt for now: they are outside the submission and
# were not written to the cap; drop them from this list when they are.
[sentence_cap]
max_words = 35
fail = true
exempt_files = ["main.tex", "main-eprint.tex", "refs.bib",
  "sec/app-defs.tex", "sec/app-measure.tex", "sec/app-stats.tex", "sec/app-threats.tex",
  "sec/ep-related.tex", "sec/ep-limits.tex"]

# ---------------------------------------------------------------- duplicated prose
# A duplicate is not a contradiction, which is why it passes every other rule here,
# and it is precisely how a claim comes to be corrected in one place and left standing
# in another. That is the failure class that broke rounds 6 to 8. The restructure to
# the venue's page limit moves more text than any previous one, so this rule goes in
# before the text does.
#
# main.tex is exempt because an abstract restates the body by design. Generated files
# and numbers.tex are exempt because they are data, not prose.
[duplicate]
window = 12
# shared/front.tex carries the title and abstract, which restate the body by design.
exempt_files = ["main.tex", "main-eprint.tex", "shared/front.tex"]
allow = []

# ---------------------------------------------------------------- stated once
# The venue's arrangement puts each limit beside the claim it bounds rather than in
# one trailing section. docs/review-standard.md created the single-place rule because
# rounds 6 to 8 generated their own work through caveat proliferation, so distributing
# them is only safe if a machine counts. Each phrase below is a limit or a scope
# statement that must be made exactly once, wherever it is made.

[[once]]
phrase = "a co-located attacker exploiting a shared cache, and a fault channel, are out of scope"
why = "the remit paragraph owns the channel boundary; section 2 stated it a second time"

[[once]]
phrase = "no analyser, no measurement and no hardware"
why = """The cold-clone claim is the artifact pitch and belongs to the Artifact run-in.
The method section describes the same split as a mechanism and had restated the tail
verbatim, eight words, which is under the duplicate rule's window. The phrase this rule
watches changed in round 15: it used to end "no containers", which was false for five of
the six recall-eligible pairs, and the retired entry above keeps the old wording out."""

[[once]]
phrase = "has no maintainer to coordinate with"
why = "the disclosure statement owns the MatrixSSL coordination fact"

[[once]]
phrase = "ANONYMISED-LINK"
why = """The anonymised repository link the reviewer asked for at submission. The Artifact
paragraph of sec/intro.tex carries a url{ANONYMISED-LINK} placeholder the author fills by
hand before submission; registering it here means the placeholder cannot be forgotten
(the rule fails if it is deleted without a replacement being registered) and cannot be
pasted into a second file."""

[[once]]
phrase = "establishes that a blind spot exists in a class, not how often it occurs"
why = "the n=1 bound is stated in the I3 contribution bound (sec/intro.tex) and must not be re-hedged at each use"

[[once]]
phrase = "so the core frequency is not pinned and core isolation is partial"
why = "the acquisition host's bound is stated once, where the statistics are decided"

# Revision A9: the tick-to-cycle relation is stated once, in the Apparatus section beside
# the verdict rule, and the host figure's caption points at it rather than restating the
# tick length per host.
[[once]]
phrase = "a tick is coarser than the core cycle"
why = "the counter-tick unit and its relation to the core cycle are stated once, in sec/method.tex; every other site prints ticks and points there"

# ---------------------------------------------------------------- the anti-survey gate
# The analyser matrix classifies a field this paper explicitly did not survey: its rows are
# a pinned third-party inventory filtered by two of that inventory's own predicates. The
# table is scaffolding for a scoring claim, not a systematisation, and TCHES has a separate
# SoK category for work that is. A rule you can enforce beats a rule you remember.

[[forbidden]]
phrase = "we survey"
in_files = ["sec/f1-blindspots.tex"]
why = "the rows are a third-party inventory; claiming a survey would be false and would invite the paper to be read as an SoK"

[[forbidden]]
phrase = "we systematise"
in_files = ["sec/f1-blindspots.tex"]
why = "same"

[[forbidden]]
phrase = "our classification of the field"
in_files = ["sec/f1-blindspots.tex"]
why = "same: the classification axes are ours, the rows are not"

# The four indices are numbered I1 to I4 in reading order (build cell, host, analyser and
# budget, fix site and facet). The old F1/F2/F3 labels were never defined in the text and
# ran against reading order (F2, F3, then F1), which a reviewer noticed. They are refused
# everywhere in the body so they cannot come back in a section title or a cross-reference.
[[forbidden]]
phrase = "F1:"
in_files = ["sec/f1-blindspots.tex", "sec/f2-toolchain.tex", "sec/f3-microarch.tex", "sec/intro.tex", "sec/index.tex", "sec/method.tex", "sec/fixes.tex", "sec/conclusion.tex", "sec/background.tex", "shared/front.tex"]
why = "the indices are I1 to I4 in reading order; F1/F2/F3 were undefined labels in section titles"

[[forbidden]]
phrase = "F2:"
in_files = ["sec/f1-blindspots.tex", "sec/f2-toolchain.tex", "sec/f3-microarch.tex", "sec/intro.tex", "sec/index.tex", "sec/method.tex", "sec/fixes.tex", "sec/conclusion.tex", "sec/background.tex", "shared/front.tex"]
why = "same"

[[forbidden]]
phrase = "F3:"
in_files = ["sec/f1-blindspots.tex", "sec/f2-toolchain.tex", "sec/f3-microarch.tex", "sec/intro.tex", "sec/index.tex", "sec/method.tex", "sec/fixes.tex", "sec/conclusion.tex", "sec/background.tex", "shared/front.tex"]
why = "same"

# ---------------------------------------------------------------- cannot both be true
# The register of claim pairs that have contradicted each other once. The duplicate and
# once rules cannot see this class, because the two statements share no wording: they are
# opposites, not copies. All three entries below shipped on 2026-08-26 in a single day of
# editing, each because a claim was updated in one place and its opposite left standing in
# another, and all three were found by a blind referee rather than by any gate here.

[[contradiction]]
claim = "we do not establish that the deployed signing path executes it"
against = "no longer an inference"
why = """The abstract denied the deployment link while the fix section asserted it. The
paper now states the intermediate claim in both: source dispatch and a whole-signature cost
budget place a routine costing all but the measured gap (mxGenkeyGap) of the signing path's
own key generation inside the signature, and the residual was measured on that routine
called directly. "Indistinguishable" is not used for that gap: it is consistent in sign
across every containment run."""

[[contradiction]]
claim = "each arm is one acquisition"
against = "acquired \\mxReps{} times"
why = """The ladder figure's caption still described the single-acquisition regime after the
rebuild replaced it with three acquisitions per design, and in the same caption claimed to
draw no ratio while printing one."""

[[contradiction]]
claim = "says which three groups those are"
against = "after the rebuild two remain"
why = """The introduction promised three groups of non-recomputable quantities and the fix
section named two. The count is now stated in exactly one place and pointed at from the
other."""

# ---------------------------------------------------------------- item A5, background
# The novelty is stated once, in the background section, as per-pair certified labels that
# carry their indices; the introduction points at it rather than restating it.
[[once]]
phrase = "per-pair certified labels that carry"
why = "the novelty statement lives in sec/background.tex and is pointed at from elsewhere"

# One paper cited under two keys as if it were two works: the usability study of Fourne et
# al. was cited as usect and again, characterised as a separate developer study, as
# usectusability. The second characterisation is withdrawn with the key; the work keeps one
# sentence under usect.
[[retired]]
phrase = "a developer study finds every tool it tried affected by problems that prevent effective"
retired_in = "sec/background.tex, item A5"
why = "this described the same paper already cited in the preceding clause under a second key; the merged citation says what the study did once"

# The implementer recommendation once prescribed a retired-instruction count over two secret
# classes as a sufficient check. By the paper's own data that check passes the residual it
# found: the instructions saved per leading zero are the same to within about one percent
# whether one or sixty-three are skipped, while the clock over that range moves by nearly two
# orders of magnitude. Count balance is necessary, not sufficient.
[[retired]]
phrase = "count retired instructions over two secret classes"
retired_in = "sec/conclusion.tex, revision A8"
why = "a check that passes the paper's own headline residual cannot be recommended as the check; the recommendation now asks for cycle-level measurement or operand-magnitude fuzzing at a fixed digit count"

[[retired]]
phrase = "recovery pending"
retired_in = "tab:fixes and the abstract, the fifteenth review's E2"
why = """The paper said the MatrixSSL residual's exploitability was pending a timing-ordered
lattice attempt on the fixed build. Nine such attempts are now recorded
(results/matrixssl_recovery.json) and none recovers the key, for a structural reason the
record states: the published attack's rank model assumes a multi-bit bias where the fix
leaves one bit. The outcome is a measured, bounded observation and is stated as one."""

[[retired]]
phrase = "no timing-ordered lattice attempt"
retired_in = "sec/fixes.tex and sec/app-measure.tex, the fifteenth review's E2"
why = """Same as 'recovery pending': the attempt has been made, nine times, and is on record."""

[[retired]]
phrase = "to \\mxRepNHi{} records"
retired_in = "sec/method.tex, round 2 of this cycle"
why = """The repeat dumps each hold the full dudectBudget (59,967) records, the first
byte-identical to the committed single dump; mxRepNLo to mxRepNHi (56,968 to 56,970) is the
effect estimator's post-95%-crop sample size, not a record count. Any prose calling it a
record count is the retired story, twice re-found before it was rooted out."""

[[retired]]
phrase = "dumps with dropped deltas"
retired_in = "bin/dudect_permute.py docstring, round 3 of this cycle"
why = """No committed dump dropped a delta: every corpus and MatrixSSL dump holds exactly
59,967 records (19,989 x 3), as the sidecars record. The 56,968 figure is a 95% crop, not a
dropped-delta remainder."""

# ---- sixteenth review: retired wordings, each corrected at its source ----
[[retired]]
phrase = "error-tolerant by construction"
retired_in = "results/matrixssl_recovery.json and recover.py literals, the sixteenth review"
why = """The vendored Minerva attack sorts by elapsed time and bakes a rank-derived leading-zero
bound into every lattice row with no slack, no subset retry and no outlier rejection, so it
tolerates no misclassification. A budget bound for an error-tolerant attack is recorded instead
(results/matrixssl_budget_bound.json) and the row stays incomplete."""

[[retired]]
phrase = "timing-ordered and error-tolerant"
retired_in = "recover.py docstring, the sixteenth review"
why = "the two-adjective spelling of the same false framing; the instrument is timing-ordered but not error-tolerant"

[[retired]]
phrase = "there is no hardware division"
retired_in = "sec/intro.tex, the sixteenth review"
why = """The Cortex-A7 implements UDIV and SDIV. The software routine the published attack timed
comes from the 32-bit build target's ISA baseline, which is the build-cell index acting at a
third target, not a property of the core."""

[[retired]]
phrase = "from the remote vantage"
retired_in = "sec/index.tex, the sixteenth review"
why = """The published attacks do not share a vantage: KyberSlash and the ladder are local, and
Minerva's library signatures were collected by a local C program on a laptop (eprint 2020/728).
Only the MAC comparison is a remote attack."""

[[retired]]
phrase = "neither carries an"
retired_in = "sec/index.tex Definition 2, the sixteenth review"
why = "Leak(b,h) is now an explicit abbreviation for exists n Leak(b,h,n); the arity overload is gone"

[[retired]]
phrase = "of a full call"
retired_in = "shared/front.tex, the sixteenth review"
why = "the call is the 2.05-tick coeff_to_bit call amortised over a batch, not a decapsulation; name it"

[[retired]]
phrase = "into a full call"
retired_in = "sec/f3-microarch.tex, the sixteenth review"
why = "same as 'of a full call'"

[[retired]]
phrase = "in a full call"
retired_in = "sec/intro.tex, the sixteenth review"
why = "same as 'of a full call'"

[[retired]]
phrase = "minimum cycles"
retired_in = "sec/fixes.tex, the sixteenth review"
why = "the libgcrypt probe reads rdtscp; its figures are TSC ticks and the record's key name is a misnomer"

[[retired]]
phrase = "libgcrypt: the same patch"
retired_in = "sec/fixes.tex paragraph heading, the sixteenth review"
why = "the measured claim is scoped to 1.8.4 against 1.8.5; the heading now names the release"

[[retired]]
phrase = "an ablation build we did not make"
retired_in = "sec/fixes.tex, the sixteenth review"
why = "the mechanism sentence is stated as a source-level hypothesis in the register the wolfSSL case uses; the ablation is planned"

[[retired]]
phrase = "closed where a tool looks"
retired_in = "sec/conclusion.tex, the sixteenth review"
why = "the opposite of the finding: libgcrypt is closed along the deployed path and unchanged at the primitive a tool drives"

[[retired]]
phrase = "yields a declared boundary"
retired_in = "sec/conclusion.tex, the sixteenth review"
why = "wolfSSL is not graded; the boundary account moved to the eprint with the attempt"

[[retired]]
phrase = "outside the method's domain"
retired_in = "sec/fixes.tex, the sixteenth review"
why = "the wolfSSL paragraph moved to the eprint as an attempted reproduction; the body keeps one sentence"

[[retired]]
phrase = "recovery fails on both arms"
retired_in = "sec/fixes.tex table row, the sixteenth review"
why = "the wolfSSL row now reads not graded with a pointer to the recorded attempt"

[[retired]]
phrase = "the admission rule is not met"
retired_in = "shared/front.tex and sec/intro.tex, the sixteenth review"
allow_in = ["sec/ep-limits.tex"]
why = "the abstract and introduction say 'wolfSSL is not graded'; the rule is named where the attempt is recorded"

[[retired]]
phrase = "a boundary of the method rather than a property of the patch"
retired_in = "sec/intro.tex, the sixteenth review"
allow_in = ["sec/ep-limits.tex"]
why = "stated once, beside the recorded attempt"

[[forbidden]]
phrase = "tier-C"
in_files = ["shared/front.tex"]
why = "a term defined in section 3 must not carry the abstract"

[[forbidden]]
phrase = "fix-site"
in_files = ["shared/front.tex"]
why = "a term defined in section 3 must not carry the abstract"

[[once]]
phrase = "witnesses the existential"
why = "the soundness assumption linking the modelled host set to real hosts is stated once, in section 3"

[[once]]
phrase = "discriminates nothing"
why = "the per-call design's verdict on its own arrangement is reconciled once, in section 7"

[[once]]
phrase = "point estimate with no null"
why = "the min-of-batches estimator's status is stated once, in section 5"

[[once]]
phrase = "not a property of a core"
why = "the Cortex-A7 routine as a build-target effect is stated once, in section 5"

[[once]]
phrase = "do the equivalent per run"
why = "the RTLF contrast is stated once, in section 7"

[[once]]
phrase = "where the index bites"
why = "section 7's evaluation size and shape is stated once"

[[once]]
phrase = "knowledge of what they planted"
why = "the SCOUT-CT proximity and the difference are stated once, in section 2"

[[once]]
phrase = "names the intended design"
why = "libgcrypt's relocated outcome as Minerva's own mitigation is stated once, in section 8"

[[once]]
phrase = "cannot account for"
why = "the instruction-spread arithmetic is stated once, in section 9"

[[once]]
phrase = "build configuration is unrecorded"
why = "the wolfSSL attempt's missing configuration is stated once"

[[once]]
phrase = "a tick is coarser than the core cycle"
why = "the tick-to-cycle caveat lives in section 6 only"

[[retired]]
phrase = "would settle it"
why = "the mechanism ablation was built and measured on 2026-09-02 (results/matrixssl_ablation.json); the hedge that an ablation build would settle which term dominates is no longer a hedge"

[[retired]]
phrase = "Which dominates is a statement about the code"
why = "measured by ablation on 2026-09-02: the fixed operands account for the per-iteration gap and the copies are nearly free"
