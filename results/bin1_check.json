{
  "control": "BIN-1",
  "finding": "every locked build cell rebuilds to the .text digest recorded for it, so the pinned cells behind I1 are reproducible rather than asserted from the lock.",
  "binaries_checked": 80,
  "pairs": [
    "_sentinel-negative",
    "_sentinel-positive",
    "hmac-timing",
    "hqc-reject",
    "kyberslash"
  ],
  "discrepancies": 0,
  "generator": "bin/build.py --check",
  "note": "Run on demand, not in the enforced control loop: a full rebuild of every cell inside the pinned images is minutes of work. The count is arms x cells over the pairs listed."
}
